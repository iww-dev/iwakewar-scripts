local LP = game.Players.LocalPlayer local hum = LP.Character:FindFirstChildOfClass("Humanoid") local seat = nil for _, v in ipairs(game.Workspace.Blocks.Sfsxs0:GetDescendants()) do if v:IsA("Seat") or v:IsA("VehicleSeat") then seat = v break end end if not seat then return "no seat" end local hrp = LP.Character:FindFirstChild("HumanoidRootPart") hrp.CFrame = seat.CFrame + Vector3.new(0, 3, 0) task.wait(0.5) pcall(function() seat:Sit(hum) end) task.wait(1) local sat = hum.SeatPart ~= nil local z = game.Workspace:FindFirstChild("Really redZone") z.VoteLaunchRE:FireServer() task.wait(1.5) local lr = z:FindFirstChild("LaunchRequirments") local p = lr and lr:FindFirstChild("Pressed") return { seated = sat, pressed = p and p.Value, launched = z.Launched.Value }