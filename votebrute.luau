local z = game.Workspace:FindFirstChild("Really redZone") local out = {} local function st() local lr = z:FindFirstChild("LaunchRequirments") local p = lr and lr:FindFirstChild("Pressed") return p and p.Value end table.insert(out, "pressed0=" .. tostring(st())) local tc = z:FindFirstChild("TeamColor") for _, args in ipairs({ {"Really red"}, {tc and tc.Value}, {game.Players.LocalPlayer.Name}, {"Yes"}, {true} }) do if args[1] ~= nil then pcall(function() z.VoteLaunchRE:FireServer(table.unpack(args)) end) task.wait(0.8) table.insert(out, tostring(args[1]):sub(1,30) .. "->" .. tostring(st())) end if job.cancelled() then break end end table.insert(out, "launched=" .. tostring(z.Launched.Value)) return out