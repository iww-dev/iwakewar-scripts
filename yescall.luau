local btn = game.Players.LocalPlayer.PlayerGui.LaunchBoatGui.PromptLaunch.Yes local out = {} for _, c in ipairs(getconnections(btn.MouseButton1Click)) do local ok, err = pcall(function() c.Function() end) table.insert(out, tostring(ok) .. ":" .. tostring(err):sub(1, 80)) end task.wait(1.5) local z = game.Workspace:FindFirstChild("Really redZone") local lr = z:FindFirstChild("LaunchRequirments") local p = lr and lr:FindFirstChild("Pressed") table.insert(out, "pressed=" .. tostring(p and p.Value) .. " launched=" .. tostring(z.Launched.Value)) return out